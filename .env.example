# Copy to .env.local and paste your key, or let the parent env feed it:
# this app lives in secondtalent/, and the real key lives in secondtalent/.env.
# Setup copies OPENAI_API_KEY from ../.env into .env.local (which is gitignored).
OPENAI_API_KEY=sk-your-openai-api-key-here
